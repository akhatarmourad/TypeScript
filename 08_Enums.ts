/* Enumerations in TypeScript */
enum CourseChoices {
    NEXTJS,
    REACTJS,
    REDUX,
    NESTJS,
    JEST
}

const myChoice = CourseChoices.REDUX;

enum Softwares {
    POWERBI = 10,
    TABLEAU,
    EXCEL = "MicroSoft Excel",
    OFFICE = 45,
}